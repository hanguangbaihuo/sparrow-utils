/**
 * sum函数用来计算两个数字相加，并返回它们的和
 * @param {number} [a]-一个数字
 * @param {number} [b]-一个数字
 * @returns {number}-一个数字
 * @example
 * // returns 2
 * sum(1, 1);
 */
function sum(a: number, b: number): number {
  console.log(111);
  return a + b;
}

export default sum;
