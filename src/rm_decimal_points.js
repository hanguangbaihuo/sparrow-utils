/**
 * 除去小数点
 * 
 * @author junchao
 * @date 2019-07-16
 */

/**
 * 除去小数点
 * 
 * @param {Number} num 要处理的数字
 */
export const removeDecimalPoints = function removeDecimalPoints(num) {
  return Math.floor(num);
};